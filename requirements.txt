# CPU-only PyTorch for Vercel size optimization
--index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
transformers>=4.30.0

# Core Flask application
flask>=2.3.0
flask-cors>=4.0.0

# Essential dependencies
numpy>=1.21.0
requests>=2.25.0

# Optimized for Vercel 250MB limit with CPU-only inference