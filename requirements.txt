# ONNX Runtime for optimized ML inference (lightweight)
onnxruntime>=1.15.0

# Core Flask application
flask>=2.3.0
flask-cors>=4.0.0

# Essential dependencies
numpy>=1.21.0
requests>=2.25.0

# Optimized for Vercel 250MB limit with ONNX model (~60-80MB total)